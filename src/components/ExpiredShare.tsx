// src/components/ExpiredShare.tsx
import './ExpiredShare.css';

export default function ExpiredShare() {
  return (
    <div className="expired-share">
      <p>This share has expired and can no longer be viewed.</p>
    </div>
  );
}